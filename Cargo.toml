[package]
name = "actor-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
async-broadcast = { version = "0.7.2", default-features = false }
async-channel = { version = "2.5.0", default-features = false }
async-executor = { version = "1.14.0", default-features = false }
async-io = { version = "2.6.0", default-features = false }
#TODO: remove and use futuresunordered instead? Use this in the bin later on?
futures-concurrency = { version = "7.7.1", default-features = false, features = ["std"] }
futures-core = { version = "0.3.32", default-features = false }
futures-util = { version = "0.3.32", default-features = false, features = ["alloc"] }
oneshot = { version = "0.2.1", features = ["async", "std"] }
pin-project = { version = "1.1.13", default-features = false }
signal-hook = { version = "0.4.4", default-features = false, features = ["iterator"] }
snafu = { version = "0.9.0", default-features = false, features = ["std"] }
static_assertions = "1.1.0"
tracing = { version = "0.1.44", default-features = false, features = ["attributes"] }
tracing-error = { version = "0.2.1", default-features = false }

[dev-dependencies]
futures-test = "0.3.32"
tracing-subscriber = "0.3.23"

[patch.crates-io]
#NOTE: i dont like this crate: it has bugs like polling depleted streams more than once,
#it is missing things that should be there, like Fuse not implementing FusedStream. Even
#though i like the simplified API over futures, i will not use it since its so buggy. For
#example, the StreamExt::race never terminates.
futures-lite = {path="../futures-lite"}
